@model BoardPreview


<a asp-page="/Board/Index" asp-route-boardCode="@Model.BoardId"
   class="list-group-item list-group-item-action bg-primary text-white">@Model.BoardName</a>

@foreach (var post in Model.PostList)
{
    <a class="list-group-item list-group-item-action">@post.Title</a>
}